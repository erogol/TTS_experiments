#!/bin/bash
yes | apt-get install sox
yes | apt-get install ffmpeg
yes | apt-get install espeak
yes | apt-get install tmux
yes | apt-get install zsh
# install oh my zsh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"
sudo sh install.sh
# move LJspeech
mkdir ~/LJSpeech-1.1
cp -R /data/ro/shared/data/keithito/LJSpeech-1.1/wavs /root/LJSpeech-1.1
cp -R /data/ro/shared/data/keithito/LJSpeech-1.1/meta* /root/LJSpeech-1.1
# miniconda
cp -R /data/rw/home/miniconda3 /root/

echo "exec zsh" >> ~/.bashrc
echo "__conda_setup="$('/root/miniconda3/bin/conda' 'shell.bash' 'hook' 2\> /dev/null)"
if [ $? -eq 0 ]; then
    eval '$__conda_setup'
else
    if [ -f '/root/miniconda3/etc/profile.d/conda.sh' ]; then
        . '/root/miniconda3/etc/profile.d/conda.sh'
    else
export PATH='/root/miniconda3/bin:$PATH'
    fi
fi
unset __conda_setupgit checkout" >> ~/.zshrc
# down pytorch
while true; do sleep 1000000; done
